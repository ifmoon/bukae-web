import { styled } from '../stitches.config';

const SplashWrapper = styled('div', {
  width: '100%',
  height: '100%',
  backgroundColor: '$neon',
});

const Splash = () => {
  return <SplashWrapper></SplashWrapper>;
};

export default Splash;
